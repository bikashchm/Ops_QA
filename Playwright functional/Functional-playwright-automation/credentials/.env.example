# Environment selector: qa | uat | stage | prod
TEST_ENV=uat

# Override any value from qa.env / stage.env / prod.env locally (gitignored file)
# Copy credentials/.env.local.example to credentials/.env.local
